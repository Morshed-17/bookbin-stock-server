const { Router } = require("express");

const router = Router();


//* auth routes

router.use("/auth", () => {});


//* items routes
router.use("/items", () => {})


//* users routes
router.use("/users", () => {})

module.exports = router;
